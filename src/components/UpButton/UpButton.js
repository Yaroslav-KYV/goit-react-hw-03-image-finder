import styled from 'styled-components';
import styledVar from '../../variableStyle/styled-variables';

const UpBtn = styled(styledVar.RoundBtn)`
  position: fixed;
  bottom: 2%;
  left: 96%;
`;

export default UpBtn;
